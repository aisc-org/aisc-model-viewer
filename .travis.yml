language: node_js
node_js:
  - node
os:
  - linux
dist: bionic

install:
  - yarn install
script: 
  - yarn build

# Publish the website
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist
  target_branch: gh-pages
  on:
    branch: master
